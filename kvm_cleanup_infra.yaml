- name: Clean up terraform deployment
  hosts: kvm_host
  become: true
  tasks:
    - include_role:
        name: kvm_cleanup
      vars:
        vm_hostname: "{{ item.vm_hostname }}"
      with_items: "{{ infra }}"
